openapi: 3.0.0
info:
  title: Essence Enclave API
  description: API documentation for Essence Enclave backend
  version: 1.0.0

servers:
  - url: http://localhost:5000
    description: Development server

tags:
  - name: Auth
    description: Authentication endpoints
  - name: Users
    description: User management

paths:
  /api/auth/register:
    post:
      tags:
        - Auth
      summary: Register a new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterUser"
      responses:
        201:
          description: User registered successfully
        400:
          description: Validation error

  /api/auth/login:
    post:
      tags:
        - Auth
      summary: Login into your account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginUser"
      responses:
        200:
          description: Logged in successfully
        400:
          description: Validation error

  /api/auth/logout:
    get:
      tags:
        - Auth
      summary: Logout your account
        content:
          application/json:
      responses:
        200:
          description: Logged out successfully
        400:
          description: Validation error

  /api/auth/user:
    get:
      tags:
        - Users
      summary: Fetched user informations
        content:
          application/json:
      responses:
        200:
          description: User details fetched successfully
        400:
          description: Validation error

  /api/auth/refresh:
    get:
      tags:
        - Auth
      summary: Refresh Token
        content:
          application/json:
      responses:
        200:
          description: Token generated successfully
        400:
          description: Validation error
components:
  schemas:
    RegisterUser:
      type: object
      required:
        - firstName
        - lastName
        - email
        - password
      properties:
        firstName:
          type: string
          example: John
        lastName:
          type: string
          example: Doe
        email:
          type: string
          example: johndoe@gmail.com
        password:
          type: string
          example: secret123
        phoneNumber:
          type: string
          example: "08012345678"

    LoginUser:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          example: johndoe@gmail.com
        password:
          type: string
          example: secret123

